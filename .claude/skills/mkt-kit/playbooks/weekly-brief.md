# Weekly brief

Harness-neutral. Copy these steps into the session checklist verbatim. Keep skipped steps listed as `skip: <reason>`.

Named existing verb: `/generate-week` (Typefully weekly social from blog/transcript — `.claude/commands/generate-week.md`). Not a new planner.

If the user asked for a two-week execution plan (10–14 pieces, shoot list): skip step 3 with `skip: biweekly cadence — use /generate-biweekly` and stop. Do not write a biweekly playbook. Do not invent a third week.

## Steps

1. Load `mkt-kit` pack for this silo.
2. Run `content-note-status-sweep` (required before any new brief).
3. Run `/generate-week` with the user's source (blog URL, transcript, themes).
4. Do not publish, schedule, or flip notes to approved.
